https://bingo-be-h0j9.onrender.com

## Running
- Ensure you have python 3.10+ and docker/compose.
- Create a venv and install the packages in requirements.txt with pip.
- Run the following in the terminal:
```
docker compose up
```
- Server can now be accessed from http://localhost:8000
